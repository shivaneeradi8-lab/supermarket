{
  "name": "client-mk-workspace",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix my-website",
    "dev": "concurrently -n client,server -c green,blue \"npm run dev:client\" \"npm run dev:server\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
